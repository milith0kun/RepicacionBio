process {
  executor = "pbspro"
  
  queue = "general_cloud"

  withLabel: "colabdesign|rfdiffusion" {
    queue = "gpu_a10g"
    clusterOptions = "-l select=1:ncpus=4:mem=20gb"
    // beforeScript = "..." // commands to run at the start of each job
  }
  
}

